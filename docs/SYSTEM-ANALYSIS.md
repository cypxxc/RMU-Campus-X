# รายงานวิเคราะห์ระบบ RMU-Campus X

วิเคราะห์ฟังก์ชันและ logic ที่แปลก ไม่ครบถ้วน หรือมีความเสี่ยงด้านความปลอดภัย

---

## 🔴 รุนแรง (Security / Logic ผิดพลาด)

### 1. **PATCH `/api/exchanges/[id]` – ไม่มีการตรวจสอบตัวตน**

**ไฟล์:** `app/api/exchanges/[id]/route.ts`

**ปัญหา:**  
- ไม่มี `withValidation` หรือ `verifyIdToken`  
- รับ `userId` จาก request body แล้วใช้เป็นคนทำรายการ  
- ผู้โจมตีสามารถส่ง `userId` ปลอมและ `status` ใดก็ได้ เพื่ออัปเดต exchange โดยไม่ต้องมี token

**ผลกระทบ:**  
ใครก็ได้ที่รู้ `exchangeId` สามารถเปลี่ยนสถานะการแลกเปลี่ยน (เช่น เป็น `completed` หรือ `cancelled`) ได้

**แนวทางแก้:**  
- ใช้ `withValidation` หรืออย่างน้อย `verifyIdToken(token, true)`  
- ใช้ `decodedToken.uid` เป็น `userId` (ห้ามใช้จาก body)  
- ตรวจว่า `userId` เป็น `ownerId` หรือ `requesterId` ของ exchange นั้น  
- ใช้ state machine (`validateTransition`) ก่อนอัปเดตสถานะ  

---

### 2. **GET `/api/exchanges/[id]` – ไม่มี Auth และไม่ตรวจสิทธิ์**

**ไฟล์:** `app/api/exchanges/[id]/route.ts`

**ปัญหา:**  
- ไม่ตรวจสอบ token  
- ไม่ตรวจว่า caller เป็น owner หรือ requester  

**ผลกระทบ:**  
ใครก็ได้ (รวมถึงไม่ได้ล็อกอิน) ที่รู้ `exchangeId` สามารถดึงรายละเอียดการแลกเปลี่ยน (owner, requester, item ฯลฯ) ได้

**แนวทางแก้:**  
- บังคับ require auth  
- ตรวจว่า `decodedToken.uid` เป็น `ownerId` หรือ `requesterId` ของ exchange นี้ ถ้าไม่ใช่คืน 403  

---

### 3. **termsAccepted – ตรวจแค่ฝั่ง UI**

**ปัญหา:**  
- Firestore rules ไม่ได้เช็ค `termsAccepted`  
- API ทุกตัวใช้แค่ `verifyIdToken(token, true)` (เช็ค status ACTIVE/WARNING) ไม่ได้เช็ค terms  

**ผลกระทบ:**  
ถ้ามีคน bypass หน้า consent (เช่น เรียก API โดยตรง) สามารถใช้งานระบบได้โดยไม่เคยยอมรับข้อกำหนด/นโยบายความเป็นส่วนตัว

**แนวทางแก้ (เลือกทำ):**  
- เพิ่มเงื่อนไขใน Firestore rules ว่าให้ write บาง collection ได้เฉพาะเมื่อ user มี `termsAccepted == true` (ถ้าต้องการบังคับที่ rules)  
- หรือเพิ่ม middleware/helper ฝั่ง API: อ่าน user doc แล้วเช็ค `termsAccepted` ก่อนทำ action สำคัญ  

---

## 🟠 ปานกลาง (Logic ไม่ครบ / ไม่สอดคล้อง)

### 4. **การสร้าง User doc สองที่ – โครงสร้างไม่ตรงกัน**

**ที่ 1:** `lib/auth.ts` – `registerUser()`  
- สร้าง user doc พร้อม `warningCount: 0`, `restrictions: { canPost, canExchange, canChat }`

**ที่ 2:** `components/auth-provider.tsx` – เมื่อไม่มี user doc (Ghost Account)  
- สร้างแค่ `uid`, `email`, `displayName`, `photoURL`, `status`, `createdAt`, `updatedAt`  
- **ไม่มี** `warningCount`, `restrictions`

**ผลกระทบ:**  
- User ที่ถูกสร้างจาก auth-provider (Ghost recovery) จะไม่มี `restrictions`  
- โค้ดที่อ่าน `user.restrictions.canPost` อาจได้ `undefined` และต้องใช้ fallback อยู่แล้ว แต่โครงข้อมูลไม่สม่ำเสมอ

**แนวทางแก้:**  
- ตอนสร้าง user ใน auth-provider ให้ใส่ `warningCount: 0` และ `restrictions: { canPost: true, canExchange: true, canChat: true }` ให้ตรงกับ `registerUser`  

---

### 5. **updateUserProfile – type อนุญาตให้ส่ง email แต่ Rules ห้ามอัปเดต**

**ไฟล์:** `lib/db/users-profile.ts`

**ปัญหา:**  
- Type: `Partial<{ displayName, photoURL, email, bio }>` อนุญาตให้ส่ง `email`  
- Firestore rules: `notUpdating(['email', ...])` → การอัปเดต `email` ถูกปฏิเสธ  

**ผลกระทบ:**  
- ผู้ใช้หรือโค้ดที่ส่ง `email` มาอัปเดตจะได้ permission denied โดยไม่ชัดว่าเป็นเพราะฟิลด์ `email`  
- สร้างความสับสนระหว่าง type กับพฤติกรรมจริง

**แนวทางแก้:**  
- ลบ `email` ออกจาก type ของ `updateUserProfile` หรือ  
- ไม่ merge `email` เข้า payload ที่ส่งไป Firestore (strip ก่อน write) และอัปเดต JSDoc ว่า "การเปลี่ยน email ต้องทำผ่าน Auth/ระบบอื่น"  

---

### 6. **Exchange State Machine – action "confirm" ไม่แยกว่าเป็นขั้นไหน**

**ไฟล์:** `lib/exchange-state-machine.ts` – `getAllowedActions()`

**ปัญหา:**  
- สถานะ `accepted` และ `in_progress` คืน `['cancel', 'confirm']` เหมือนกันทั้ง owner และ requester  
- ในระบบจริง: "confirm" อาจหมายถึง (1) เริ่มดำเนินการ (accepted → in_progress) หรือ (2) ยืนยันเสร็จ (ทั้งคู่ confirm → completed)  
- ฟังก์ชันไม่แยกว่าตอนนี้ควรเป็น "เริ่มดำเนินการ" หรือ "ยืนยันเสร็จ"

**ผลกระทบ:**  
- UI หรือ logic ที่ใช้ `getAllowedActions` อาจแสดงปุ่มหรือ action ไม่ตรงกับ business rule  
- ไม่รุนแรงถ้า business จริงใช้แค่ "ทั้งคู่กด confirm = completed" อย่างเดียว แต่ชื่อ action ไม่ชัด

**แนวทางแก้:**  
- แยก action เป็น `startProgress` (accepted → in_progress) กับ `confirmComplete` (in_progress → completed) ถ้า business แยกสองขั้น  
- หรือเก็บแค่ `confirm` แต่เขียน comment / doc ว่าในแต่ละ status หมายถึงอะไร  

---

### 7. **PATCH `/api/exchanges/[id]` – ไม่ใช้ State Machine**

**ไฟล์:** `app/api/exchanges/[id]/route.ts`

**ปัญหา:**  
- อัปเดต `status` ตามที่ส่งมาโดยตรง  
- ไม่เรียก `validateTransition(currentStatus, newStatus)`  

**ผลกระทบ:**  
แม้จะแก้ให้มี auth แล้ว ก็ยังอาจเปลี่ยนสถานะข้ามขั้นที่ไม่อนุญาตได้ (เช่น pending → completed) ถ้าไม่ validate

**แนวทางแก้:**  
- ก่อนอัปเดต exchange ให้เรียก `validateTransition(previousStatus, status)`  
- ถ้าไม่ผ่าน คืน 400 พร้อมข้อความจาก state machine  

---

## 🟡 เล็กน้อย / ปรับปรุงได้

### 8. **restrictions – ใช้แค่ฝั่ง Client**

**ปัญหา:**  
- `use-account-status.ts` เช็ค `restrictions.canPost`, `restrictions.canExchange` ฯลฯ  
- API ใช้แค่ `verifyIdToken(token, true)` ซึ่งดูแค่ `status === ACTIVE || WARNING` ไม่ได้อ่าน `restrictions`  

**ผลกระทบ:**  
ถ้า admin ตั้ง `restrictions.canPost = false` แต่ user ยังเป็น ACTIVE ฝั่ง client จะซ่อนปุ่มโพสต์ แต่ถ้าเรียก API สร้าง item โดยตรง (ด้วย token ที่ยัง ACTIVE) อาจผ่านได้ เพราะ Firestore rules ใช้ `canUseApp()` (ดู status) ไม่ได้ดู `restrictions`

**หมายเหตุ:**  
Firestore rules ปัจจุบันไม่ได้เช็ค `restrictions` ดังนั้นการบังคับต้องทำที่ API หรือเพิ่มใน rules

**แนวทางแก้ (ถ้าต้องการบังคับจริง):**  
- ใน API ที่สร้าง item / สร้าง exchange ฯลฯ อ่าน user doc แล้วเช็ค `restrictions.canPost` / `canExchange`  
- หรือเพิ่มเงื่อนไขใน Firestore rules ว่าให้ create item ได้เฉพาะเมื่อ `resource.data` หรือ user doc ผ่านเงื่อนไขที่เกี่ยวกับ restrictions (ต้องออกแบบ rules ใหม่)  

---

### 9. **GET `/api/exchanges/[id]` – ไม่มี Auth; PATCH ใช้ได้โดยไม่รู้ว่าเรียกจากที่ไหน**

**ปัญหา:**  
- README ระบุว่า `PATCH /api/exchanges/[id]` ใช้อัปเดตสถานะ  
- ในโค้ดไม่มีที่ไหนเรียก route นี้ (cancel ใช้ POST `/api/exchanges/cancel`, confirm ใช้ Firestore โดยตรง)  

**ผลกระทบ:**  
- Route อาจเป็น dead code หรือเหลือจาก design เก่า  
- ถ้าไม่ใช้ ควรลบหรือปิดการเข้าถึง; ถ้าใช้ ต้องเพิ่ม auth + state machine ตามข้อ 1 และ 7  

---

### 10. **ConsentGuard – แม้ path อนุญาต ยังอาจ flash หน้าเดิมก่อน redirect**

**ปัญหา:**  
- Guard รันฝั่ง client หลัง hydrate  
- ถ้า user ยังไม่ยอมรับและเข้า URL ที่ต้อง consent อาจเห็นเนื้อหาหน้าปลายทางสั้นๆ ก่อนถูก redirect ไป `/consent`  

**แนวทางแก้ (ถ้าต้องการ):**  
- ใช้ middleware ตรวจ path + cookie/session ว่าเคยยอมรับหรือยัง แล้ว redirect ที่ server ก่อน render  
- หรือแสดง overlay / loading บนหน้าที่ต้อง consent จนกว่าจะตรวจสอบแล้ว  

---

## สรุปลำดับความสำคัญ

| ลำดับ | รายการ | ความรุนแรง | แนะนำให้แก้ |
|------|--------|------------|-------------|
| 1 | PATCH `/api/exchanges/[id]` ไม่มี auth + รับ userId จาก body | 🔴 สูง | ใช่ (เร่งด่วน) |
| 2 | GET `/api/exchanges/[id]` ไม่มี auth/ตรวจสิทธิ์ | 🔴 สูง | ใช่ |
| 3 | termsAccepted ไม่ได้บังคับที่ Backend/Rules | 🔴 กลาง | ตามนโยบาย |
| 4 | สร้าง user doc สองที่ โครงสร้างไม่ตรงกัน | 🟠 กลาง | ใช่ |
| 5 | updateUserProfile รับ email แต่ rules ห้ามอัปเดต | 🟠 ต่ำ | ควรแก้ type/docs |
| 6 | getAllowedActions ไม่แยกว่า confirm คือขั้นไหน | 🟠 ต่ำ | ตาม product |
| 7 | PATCH exchange ไม่ใช้ state machine | 🟠 กลาง | ควรแก้ถ้าใช้ route นี้ |
| 8 | restrictions ไม่ได้เช็คใน API/Rules | 🟡 ต่ำ | ตามนโยบาย |
| 9 | PATCH `/api/exchanges/[id]` อาจเป็น dead code | 🟡 ต่ำ | ลบหรือทำให้ปลอดภัย |
| 10 | ConsentGuard อาจ flash หน้าก่อน redirect | 🟡 ต่ำ | ถ้าต้องการ UX ดีขึ้น |

---

## ✅ การแก้ไขที่ทำแล้ว (ตามรายการด้านบน)

| รายการ | สถานะ |
|--------|--------|
| PATCH `/api/exchanges/[id]` – เพิ่ม auth, userId จาก token, ตรวจ participant, ใช้ state machine | ✅ แก้แล้ว |
| GET `/api/exchanges/[id]` – เพิ่ม auth + ตรวจสิทธิ์ participant | ✅ แก้แล้ว |
| termsAccepted – เพิ่ม `requireTermsAccepted` ใน api-validation และใช้ใน POST exchanges, reports, support | ✅ แก้แล้ว |
| สร้าง user doc สองที่ – auth-provider สร้าง user เพิ่ม warningCount + restrictions | ✅ แก้แล้ว |
| updateUserProfile – type เอา email ออก, strip email ก่อน write | ✅ แก้แล้ว |
| getAllowedActions – เพิ่ม JSDoc อธิบายความหมาย confirm; PATCH ใช้ validateTransition | ✅ แก้แล้ว |
| restrictions – เพิ่ม canUserExchange/canUserPost ใน firebase-admin; POST exchanges ตรวจ canUserExchange + requesterId === ctx.userId | ✅ แก้แล้ว |
| ConsentGuard – แสดง loading จนกว่าตรวจ consent เสร็จ ลด flash | ✅ แก้แล้ว |

---

*เอกสารนี้สร้างจาก codebase ณ วันที่วิเคราะห์ อาจต้องตรวจซ้ำหลังมีการแก้ไขหรือเพิ่มฟีเจอร์*
