# ระบบตรวจสอบสถานะบัญชีและการแจ้งเตือน

## 📋 สรุปฟีเจอร์ที่เพิ่มเข้ามา

### 1. **Hook: `useAccountStatus`** (`hooks/use-account-status.ts`)

Hook สำหรับตรวจสอบสถานะบัญชีผู้ใช้ก่อนทำการดำเนินการสำคัญ

#### ฟังก์ชันหลัก:
- **`canPost()`** - ตรวจสอบว่าผู้ใช้สามารถโพสต์ของได้หรือไม่
- **`canExchange()`** - ตรวจสอบว่าผู้ใช้สามารถแลกเปลี่ยนได้หรือไม่
- **`canChat()`** - ตรวจสอบว่าผู้ใช้สามารถแชทได้หรือไม่

#### สถานะที่ตรวจสอบ:
1. **ACTIVE** - ใช้งานได้ปกติ
2. **WARNING** - ได้รับคำเตือน (แสดงข้อความเตือนแต่ยังใช้งานได้)
3. **SUSPENDED** - ถูกระงับชั่วคราว (ไม่สามารถใช้งานได้จนกว่าจะหมดเวลาระงับ)
4. **BANNED** - ถูกแบนถาวร (ไม่สามารถใช้งานได้เลย)

#### การแจ้งเตือน:
- ใช้ `toast` (Sonner) แสดงข้อความแจ้งเตือนภาษาไทย
- แสดงเหตุผลการระงับ/แบน
- แสดงวันที่หมดเวลาระงับ (สำหรับ SUSPENDED)
- แสดงจำนวนคำเตือน (สำหรับ WARNING)

---

### 2. **Component: `AccountStatusBanner`** (`components/account-status-banner.tsx`)

Banner แสดงสถานะบัญชีด้านบนของหน้า Dashboard

#### การแสดงผลตามสถานะ:

**🔴 BANNED (สีแดง)**
- แสดงเหตุผลการแบน
- ปุ่มติดต่อทีมสนับสนุน
- ไม่สามารถใช้งานฟีเจอร์ใดๆ ได้

**🟠 SUSPENDED (สีส้ม)**
- แสดงวันที่หมดเวลาระงับ
- แสดงจำนวนวันที่เหลือ
- ปุ่มติดต่อทีมสนับสนุน
- ไม่สามารถโพสต์/แลกเปลี่ยน/แชทได้

**🟡 WARNING (สีเหลือง)**
- แสดงจำนวนคำเตือน
- คำเตือนว่าอาจถูกระงับถ้าได้รับคำเตือนเพิ่ม
- ปุ่มดูรายละเอียดและติดต่อทีมสนับสนุน
- ยังใช้งานได้ปกติ

**✅ ACTIVE**
- ไม่แสดง Banner

---

### 3. **การใช้งานในหน้าต่างๆ**

#### ✅ หน้าโพสต์ของ (`app/post-item/page.tsx`)
```typescript
const { canPost } = useAccountStatus()

const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault()
  
  // ตรวจสอบสถานะบัญชีก่อนโพสต์
  const statusCheck = await canPost()
  if (!statusCheck.isAllowed) {
    return // Toast จะแสดงอัตโนมัติ
  }
  
  // ดำเนินการโพสต์ต่อ...
}
```

#### ✅ หน้ารายละเอียดของ - ขอแลกเปลี่ยน (`components/item-detail-view.tsx`)
```typescript
const { canExchange } = useAccountStatus()

const handleRequestItem = async () => {
  // ตรวจสอบสถานะบัญชีก่อนขอแลกเปลี่ยน
  const statusCheck = await canExchange()
  if (!statusCheck.isAllowed) {
    setShowRequestDialog(false)
    return
  }
  
  // ดำเนินการขอแลกเปลี่ยนต่อ...
}
```

#### ✅ หน้า Dashboard (`app/dashboard/page.tsx`)
```typescript
import { AccountStatusBanner } from "@/components/account-status-banner"

// แสดง Banner ด้านบน
{user && <AccountStatusBanner />}
```

---

## 🎯 ตัวอย่างการทำงาน

### สถานการณ์ที่ 1: ผู้ใช้ถูก BANNED
1. ผู้ใช้พยายามโพสต์ของ
2. ระบบตรวจสอบสถานะ → พบว่าถูก BANNED
3. แสดง Toast สีแดง: "บัญชีถูกระงับถาวร - เหตุผล: [เหตุผล]"
4. ยกเลิกการโพสต์ทันที
5. แสดง Banner สีแดงในหน้า Dashboard

### สถานการณ์ที่ 2: ผู้ใช้ถูก SUSPENDED
1. ผู้ใช้พยายามขอแลกเปลี่ยนของ
2. ระบบตรวจสอบสถานะ → พบว่าถูก SUSPENDED จนถึง 31/12/2025
3. แสดง Toast สีส้ม: "บัญชีถูกระงับจนถึง 31 ธันวาคม 2568 - เหลืออีก 7 วัน"
4. ยกเลิกการแลกเปลี่ยนทันที
5. แสดง Banner สีส้มในหน้า Dashboard

### สถานการณ์ที่ 3: ผู้ใช้ได้รับ WARNING
1. ผู้ใช้พยายามโพสต์ของ
2. ระบบตรวจสอบสถานะ → พบว่าได้รับ WARNING 2 ครั้ง
3. แสดง Toast สีเหลือง: "คำเตือน: บัญชีของคุณได้รับการเตือน 2 ครั้ง"
4. **ยังสามารถโพสต์ได้ตามปกติ**
5. แสดง Banner สีเหลืองในหน้า Dashboard

---

## 🔧 การตั้งค่าเพิ่มเติม

### ข้อมูลใน Firestore User Document:
```typescript
{
  status: "ACTIVE" | "WARNING" | "SUSPENDED" | "BANNED",
  warningCount: number,
  suspendedUntil: Timestamp | null,
  bannedReason: string | null,
  restrictions: {
    canPost: boolean,
    canExchange: boolean,
    canChat: boolean
  }
}
```

### การอัปเดตสถานะผู้ใช้ (Admin):
```typescript
// ระงับชั่วคราว 7 วัน
await updateDoc(doc(db, "users", userId), {
  status: "SUSPENDED",
  suspendedUntil: Timestamp.fromDate(new Date(Date.now() + 7 * 24 * 60 * 60 * 1000))
})

// แบนถาวร
await updateDoc(doc(db, "users", userId), {
  status: "BANNED",
  bannedReason: "ละเมิดกฎระเบียบหลายครั้ง"
})

// ให้คำเตือน
await updateDoc(doc(db, "users", userId), {
  status: "WARNING",
  warningCount: increment(1)
})
```

---

## ✨ ข้อดีของระบบนี้

1. **ป้องกันการใช้งานที่ไม่เหมาะสม** - ผู้ใช้ที่ถูกระงับไม่สามารถทำกิจกรรมได้
2. **แจ้งเตือนชัดเจน** - ข้อความภาษาไทยที่เข้าใจง่าย
3. **Reusable** - ใช้ Hook เดียวกันได้ทุกหน้า
4. **User-friendly** - แสดงเหตุผลและวันที่หมดเวลาระงับ
5. **Flexible** - สามารถกำหนด restrictions แบบละเอียดได้ (canPost, canExchange, canChat)

---

## 🚀 การใช้งานในอนาคต

สามารถเพิ่มการตรวจสอบในหน้าอื่นๆ ได้ เช่น:
- หน้าแชท → ใช้ `canChat()`
- หน้ารายงาน → ตรวจสอบว่าไม่ใช่ผู้ใช้ที่ถูกแบน
- หน้าโปรไฟล์ → แสดงประวัติคำเตือน

---

**สร้างเมื่อ:** 24 ธันวาคม 2568  
**เวอร์ชัน:** 1.0.0
